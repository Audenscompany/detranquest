{
  "name": "alfred-os",
  "version": "9.0.0",
  "description": "Alfred OS - Personal AI Assistant",
  "scripts": {
    "start-alfred": "cd backend && npm install && npm start",
    "start": "cd backend && npm start",
    "install-backend": "cd backend && npm install"
  }
}
